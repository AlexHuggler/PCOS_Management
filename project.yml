name: PCOS
options:
  bundleIdPrefix: alex
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "16.0"
  generateEmptyDirectories: true
  groupSortPosition: top

packages:
  RevenueCat:
    url: https://github.com/RevenueCat/purchases-ios-spm.git
    from: 5.43.0

settings:
  base:
    SWIFT_VERSION: "6.0"
    SWIFT_STRICT_CONCURRENCY: complete
    IPHONEOS_DEPLOYMENT_TARGET: "17.0"
    MARKETING_VERSION: "1.0.3"
    CURRENT_PROJECT_VERSION: "16"
    DEVELOPMENT_TEAM: 2PW989LA87
    CODE_SIGN_STYLE: Automatic

targets:
  PCOS:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    configFiles:
      Debug: Config/Debug.xcconfig
      Release: Config/Release.xcconfig
    sources:
      - path: PCOS/PCOS
        excludes:
          - "**/.DS_Store"
          - "**/.gitkeep"
          - "**/README.md"
          - "Resources/Assets.xcassets"
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: alex.PCOS
        PRODUCT_NAME: PCOS
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        SWIFT_EMIT_LOC_STRINGS: "YES"
        GENERATE_INFOPLIST_FILE: "NO"
        INFOPLIST_FILE: PCOS/PCOS/Info.plist
        TARGETED_DEVICE_FAMILY: "1"
        INFOPLIST_KEY_UIApplicationSceneManifest_Generation: "YES"
        INFOPLIST_KEY_UIApplicationSupportsIndirectInputEvents: "YES"
        INFOPLIST_KEY_CFBundleDisplayName: CycleBalance
        INFOPLIST_KEY_NSPhotoLibraryUsageDescription: "CycleBalance uses your photo library to save or compare hair growth/loss progress photos."
        INFOPLIST_KEY_NSCameraUsageDescription: "CycleBalance uses your camera to scan food barcodes and capture hair growth/loss progress photos for tracking."
      configs:
        Debug:
          SWIFT_OPTIMIZATION_LEVEL: "-Onone"
          SWIFT_ACTIVE_COMPILATION_CONDITIONS: "DEBUG"
          CODE_SIGN_ENTITLEMENTS: PCOS/PCOS.debug.entitlements
        Release:
          SWIFT_OPTIMIZATION_LEVEL: "-Owholemodule"
          SWIFT_COMPILATION_MODE: wholemodule
          CODE_SIGN_ENTITLEMENTS: PCOS/PCOS.entitlements
    dependencies:
      - package: RevenueCat
        product: RevenueCat
      - package: RevenueCat
        product: RevenueCatUI

  PCOSTests:
    type: bundle.unit-test
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: PCOS/PCOSTests
        excludes:
          - "**/.DS_Store"
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: alex.PCOSTests
        PRODUCT_NAME: PCOSTests
        GENERATE_INFOPLIST_FILE: "YES"
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/PCOS.app/$(BUNDLE_EXECUTABLE_FOLDER_PATH)/PCOS"
        BUNDLE_LOADER: "$(TEST_HOST)"
        SWIFT_VERSION: "6.0"
        SWIFT_STRICT_CONCURRENCY: complete
    dependencies:
      - target: PCOS

  PCOSUITests:
    type: bundle.ui-testing
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: PCOS/PCOSUITests
        excludes:
          - "**/.DS_Store"
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: alex.PCOSUITests
        PRODUCT_NAME: PCOSUITests
        GENERATE_INFOPLIST_FILE: "YES"
        TEST_TARGET_NAME: PCOS
        SWIFT_VERSION: "6.0"
        SWIFT_STRICT_CONCURRENCY: complete
    dependencies:
      - target: PCOS

schemes:
  PCOS:
    build:
      targets:
        PCOS: all
        PCOSTests: [test]
        PCOSUITests: [test]
    run:
      config: Debug
      commandLineArguments:
        "-billing.backendMode": true
        "revenuecat": true
    test:
      config: Debug
      gatherCoverageData: false
      targets:
        - name: PCOSTests
        - name: PCOSUITests
    archive:
      config: Release

  PCOS Local StoreKit:
    build:
      targets:
        PCOS: all
        PCOSTests: [test]
        PCOSUITests: [test]
    run:
      config: Debug
      storeKitConfiguration: PCOS/PCOS/StoreKit/PCOS.storekit
      commandLineArguments:
        "-billing.backendMode": true
        "local_storekit": true
    test:
      config: Debug
      gatherCoverageData: false
      targets:
        - name: PCOSTests
        - name: PCOSUITests
    archive:
      config: Release
